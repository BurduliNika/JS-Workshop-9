🟢 **FACTS**
Here’s a concise README suitable for your F1 drivers table project.

---

# F1 Drivers Championship Table

A simple web app that fetches current F1 drivers’ championship standings from an external API, displays them in a styled table, and allows filtering by driver name. If no driver matches, a prompt notifies the user. Built with HTML, CSS, and JavaScript.

## Features

* Fetches live driver standings from `f1api.dev`.
* Displays data in a clean, responsive table.
* Filter drivers by name with a search input.
* Alerts users when no driver is found.

## Installation

1. Clone or download the repository.
2. Open `index.html` in a browser.

> ⚠️ Note: Uses a public CORS proxy for API access. For production, replace with a backend proxy.

## Usage

1. Enter a driver’s name in the input field.
2. Click **Filter**.
3. If no match is found, a prompt will appear.
4. Clear the input to reset the table.

## Technologies

* HTML5
* CSS3
* JavaScript (ES6)
* Fetch API
